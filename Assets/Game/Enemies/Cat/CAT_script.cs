using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CAT_script : inheritor
{
    // Start is called before the first frame update
    void Start()
    {

        thing();

    }

    // Update is called once per frame
    void Update()
    {
        enemyMov();
    }
}
